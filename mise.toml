[tools]
uv = "0.11.7"
python = "3.14.4"
ruff = "0.14.5"

[tasks.install]
run = "uv pip install .[dev]"

[tasks.update]
run = "uv sync -U"

[tasks.outdated]
run = "uv pip list --outdated"

[tasks.format]
run = ["ruff format", "ruff check --fix"]

[tasks.check]
run = "uv run mypy ."
